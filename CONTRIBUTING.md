# Contributing Guidelines for Students

1. Tạo branch riêng cho bài làm.
2. Commit nhỏ, message rõ ràng.
3. Không commit `.env` hoặc API key.
4. Chạy `make lint`, `make typecheck`, `make test` trước khi nộp.
5. Viết benchmark report trong `reports/benchmark_report.md`.
